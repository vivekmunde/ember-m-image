import mImageFetchLoader from 'dummy/utils/m-image-fetch-loader';
import { module, test } from 'qunit';

module('Unit | Utility | m image fetch loader');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mImageFetchLoader();
  assert.ok(result);
});
